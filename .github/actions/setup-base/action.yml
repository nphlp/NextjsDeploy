name: Setup base environment
description: Setup Bun and install dependencies with optimized caching

runs:
    using: "composite"
    steps:
        - name: Install Bun
          uses: oven-sh/setup-bun@v2
        - name: Install dependencies
          run: bun install --frozen-lockfile
          shell: bash
